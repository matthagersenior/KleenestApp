/* Legacy Home bridge intentionally disabled. The canonical owner is cores/home/home-core.js. */
(function(g){'use strict';
 if(g.KleenestHomeV3){
   /* Keep the canonical rich implementation available to home-core.js. */
   return;
 }
})(window);