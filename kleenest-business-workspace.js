/* Authoritative Business / Enterprise workspace. */
(function(){'use strict';
 const W=window.KleenestBusinessWorkspace=window.KleenestBusinessWorkspace||{};
 const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
 let business=null,membership=null,dataset='overview',locationId='all',locations=[],model=null,error='';
 const datasets=[['overview','Overview'],['locations','Locations'],['engagement','Engagement'],['qr','QR & Check-ins'],['visitors','Visitors & Retention'],['reviews','Reviews & Reputation'],['photos','Photos & Media'],['promotions','Promotions & Offers'],['campaigns','Campaigns'],['partnerships','Partnerships'],['rewards','Rewards & Redemptions'],['events','Events'],['occupancy','Occupancy'],['roi','ROI'],['growth','Growth & Intelligence'],['benchmarks','Benchmarks'],['verification','Verification & Tier']];
 async function memberships(){return await window.KleenestSupabase.businessMemberships?.()||[];}
 async function load(){error='';const ms=await memberships();membership=ms.find(x=>['owner','admin'].includes(String(x.role||'').toLowerCase()))||ms.find(Boolean)||null;business=membership?.businesses||null;if(!business){try{const d=await window.KleenestBusinessSurface?.getDashboard?.();business=d?.business||d;membership=membership||{role:'owner'};}catch(e){business=null;error=e.message||'Business unavailable';}}
  if(business){try{const ld=await window.kleenestBusinessAnalytics?.details?.(business.id,'locations');const lr=ld?.rows||[];locations=lr.map(r=>({id:r.id||r.location_id,name:r.name||r.location_name||'Unnamed location',active:r.is_active??r.active})).filter(x=>x.id);if(locationId!=='all'&&!locations.some(x=>x.id===locationId))locationId='all';const all=await window.kleenestBusinessAnalytics?.get?.(business.id,null,null,locationId==='all'?null:locationId)||[];model={analytics:all.map(x=>({...x,key:x.key,dataset:x.dataset,fields:x.fields,error:x.error}))};}catch(e){error=e.message||'Analytics unavailable';model={analytics:[]};}}
  return{business,membership,model,locations};}
 function isAdmin(){const u=window.KleenestRuntime?.user;return !!(u?.isAdmin||['owner','admin'].includes(String(membership?.role||'').toLowerCase())||String(u?.email||'').toLowerCase()==='matthagersr@gmail.com');}
 function allowed(){const u=window.KleenestRuntime?.user;return !!(u?.isAdmin||business||['enterprise'].includes(String(u?.subscriptionTier||'').toLowerCase())||String(u?.email||'').toLowerCase()==='matthagersr@gmail.com');}
 const fmt=(v,key)=>{if(v==null||v==='')return '—';if(key?.includes('pct')||key==='percentile'||key==='roi')return `${Number(v).toLocaleString(undefined,{maximumFractionDigits:2})}%`;if(key==='storage_bytes'||key==='average_size_bytes')return `${Number(v).toLocaleString()} bytes`;if(typeof v==='number')return new Intl.NumberFormat().format(v);return String(v)};
 const label=k=>String(k).replace(/_/g,' ').replace(/\b\w/g,m=>m.toUpperCase());
 function metric(key,value){return `<div class="stat"><b>${esc(fmt(value,key))}</b><span>${esc(label(key))}</span></div>`;}
 function details(rows){if(!rows?.length)return '<div class="empty">No records are available for this selection.</div>';return `<div class="detail-grid">${rows.map((r,i)=>{const entries=Object.entries(r).filter(([k,v])=>v!=null&&v!=='').slice(0,12);return `<article class="detail-card"><h4>${esc(r.title||r.name||r.label||`${label(dataset)} #${i+1}`)}</h4>${entries.map(([k,v])=>`<div class="detail-line"><span>${esc(label(k))}</span><strong>${esc(typeof v==='object'?JSON.stringify(v):fmt(v,k))}</strong></div>`).join('')}</article>`}).join('')}</div>`;}
 function body(){const row=(model?.analytics||[]).find(x=>x.key===dataset);const title=datasets.find(x=>x[0]===dataset)?.[1]||dataset;if(!row)return `<div class="card"><h3>${esc(title)}</h3><div class="message-banner error">No dataset result was returned.</div></div>`;if(row.error)return `<div class="card"><h3>${esc(title)}</h3><div class="message-banner error">${esc(row.error)}</div></div>`;const d=row.dataset||{};const fields=row.fields||window.kleenestBusinessAnalytics?.fields?.[dataset]||Object.keys(d);const selected=locationId==='all'?'All Locations':(locations.find(l=>l.id===locationId)?.name||'Selected Location');const rows=Array.isArray(d.rows)?d.rows:[];return `<div class="card"><div class="title-row"><div><h3>${esc(title)}</h3><p class="muted">${esc(selected)} · ${locationId==='all'?'business-wide':'location-specific'} calculation</p></div></div><div class="stats" style="margin-top:12px">${fields.map(f=>metric(f,d[f])).join('')}</div>${rows.length?`<h3 style="margin-top:20px">${esc(title)} details</h3>${details(rows)}`:''}</div>`;}
 function render(root){if(!root)return;if(!allowed()){root.innerHTML='<div class="card"><h1>Business</h1><p class="muted">Business access is available to business members.</p></div>';return;}root.innerHTML=`<section class="stack"><div class="title-row"><div><h1>🏢 ${esc(business?.name||'Business')}</h1><p class="muted">${esc(business?.business_tier||'standard').toUpperCase()} · ${esc(membership?.role||'owner')}</p></div><button class="ghost" data-biz-refresh>↻ Refresh</button></div>${error?`<div class="message-banner error">${esc(error)}</div>`:''}<div class="card"><label style="font-weight:800">Location</label><select id="business-location" class="input"><option value="all" ${locationId==='all'?'selected':''}>All Locations (${locations.length})</option>${locations.map(l=>`<option value="${esc(l.id)}" ${locationId===l.id?'selected':''}>${esc(l.name)}${l.active===false?' (Inactive)':''}</option>`).join('')}</select><label style="font-weight:800;display:block;margin-top:14px">Analytics dataset</label><select id="business-dataset" class="input">${datasets.map(([k,v])=>`<option value="${k}" ${dataset===k?'selected':''}>${v}</option>`).join('')}</select><p class="muted">Select a location and dataset. Metrics and records are recalculated for that selection.</p></div>${body()}</section>`;root.querySelector('#business-dataset').onchange=async e=>{dataset=e.target.value;await load();render(root)};root.querySelector('#business-location').onchange=async e=>{locationId=e.target.value;await load();render(root)};root.querySelector('[data-biz-refresh]').onclick=async()=>{await load();render(root)};}
 W.load=load;W.render=render;W.allowed=allowed;W.isAdmin=isAdmin;W.getContext=()=>({business,membership,model,locations,locationId});W.mount=async root=>{await load();render(root);};
})();