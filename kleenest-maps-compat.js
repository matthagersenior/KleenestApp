/* Legacy map API compatibility for the modular web app. */
(function(){'use strict';function refresh(reason){return window.KleenestMapDiscovery?.load?.(reason||'maps')||Promise.resolve({status:'waiting_for_location'});}function maps(action){if(action==='refresh'||action==='update-location')return refresh(action);window.KleenestNavigation?.activate?.('map');return refresh('maps-call');}maps.init=()=>refresh('init');maps.load=()=>refresh('load');maps.refresh=()=>refresh('refresh');maps.updateLocation=()=>refresh('update-location');maps.open=()=>{window.KleenestNavigation?.activate?.('map');return refresh('open');};window.maps=window.maps||maps;window.KleenestMaps=window.KleenestMaps||maps;})();
