/* Immediate modular map discovery: Kleenest locations + OpenStreetMap places around the user's location. */
(function(){'use strict';const D=window.KleenestMapDiscovery=window.KleenestMapDiscovery||{};let lastKey='',busy=false;function origin(){const l=window.KleenestLocation?.get?.()||window.KleenestRuntime?.location||window.KleenestRuntime?.userLocation||((typeof state!=='undefined')?state.location:null);if(!l)return null;const lat=Number(l.lat??l.latitude),lng=Number(l.lng??l.longitude);return Number.isFinite(lat)&&Number.isFinite(lng)?{lat,lng,accuracy:Number(l.accuracy||50)}:null;}function normalize(r){return {...r,id:String(r.id),name:r.name||'Kleenest location',address:r.address||'',lat:Number(r.latitude??r.lat),lng:Number(r.longitude??r.lng),rating:Number(r.rating||0),reviews:Number(r.review_count??r.reviews??0),cleanlinessPct:Number(r.cleanliness_pct??r.cleanlinessPct??0),accessible:!!r.accessible,changing:!!(r.changing_table??r.changing),verificationStatus:r.verification_status||r.verificationStatus||'unverified',source:r.source||'supabase',placeType:r.place_type||r.placeType||'public',brandKey:r.brand_key||r.brandKey||null,restroomConfidence:r.restroom_confidence||r.restroomConfidence||'known'};}function merge(rows,keep){if(typeof state==='undefined'||!Array.isArray(state.restrooms))return 0;const map=new Map();[...(keep||[]),...(state.restrooms||[]),...(rows||[])].forEach(raw=>{const r=normalize(raw);if(Number.isFinite(r.lat)&&Number.isFinite(r.lng))map.set(String(r.id),r);});state.restrooms=[...map.values()];try{window.render?.()}catch(e){}return rows?.length||0;}async function load(reason='bootstrap'){const o=origin();if(!o||busy)return{ok:false,reason:o?'busy':'location_unavailable'};const key=`${o.lat.toFixed(4)},${o.lng.toFixed(4)}`;if(key===lastKey&&reason!=='update-location')return{ok:true,cached:true,count:state?.restrooms?.length||0};lastKey=key;busy=true;const keep=Array.isArray(state?.restrooms)?state.restrooms.map(x=>({...x})):[];try{const promises=[];if(window.KleenestSupabase?.nearbyLocations)promises.push(window.KleenestSupabase.nearbyLocations(o.lat,o.lng,50000,200).catch(()=>[]));if(typeof window.loadRestroomsForLocation==='function')promises.push(Promise.resolve(window.loadRestroomsForLocation(o.lat,o.lng,o.accuracy)).catch(()=>[]));if(window.KleenestOSMDiscovery?.load)promises.push(window.KleenestOSMDiscovery.load(reason).then(()=>[]).catch(e=>{console.warn('[Kleenest] OSM discovery',e);return [];}));const results=await Promise.all(promises);const rows=results.flat().filter(Boolean);const count=merge(rows,keep);window.dispatchEvent(new CustomEvent('kleenest:map-discovery-ready',{detail:{reason,count,lat:o.lat,lng:o.lng}}));return{ok:true,count};}finally{busy=false;}}D.load=load;D.refresh=()=>load('update-location');function boot(){setTimeout(()=>load('startup'),0);}window.addEventListener('kleenest:location-updated',()=>load('location-updated'));window.addEventListener('kleenest:navigation-changed',e=>{if(String(e.detail?.view||'').toLowerCase()==='maps')load('maps-open')});window.addEventListener('kleenest:map-open',()=>load('maps-open'));if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();
