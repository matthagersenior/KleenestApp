/* Auth bridge for the modular Pages shell. */
(function(){'use strict';
 function setUser(session,profile){window.KleenestRuntime=window.KleenestRuntime||{};window.KleenestRuntime.user=session&&profile?{...profile,id:session.user.id,email:session.user.email,subscriptionTier:profile.subscription_tier,points:profile.points,level:profile.level,streak:profile.streak,totalCheckIns:profile.total_check_ins,totalReviews:profile.total_reviews,isAdmin:profile.is_admin,role:profile.role}:null;window.state=window.state||{};window.state.session=window.KleenestRuntime.user;}
 async function refresh(){try{const s=await window.KleenestSupabase.session();if(!s){setUser(null,null);return null;}const p=await window.KleenestSupabase.profile()||{};setUser(s,p);window.dispatchEvent(new CustomEvent('kleenest:auth-state-changed',{detail:{session:s,profile:p}}));return p;}catch(e){console.warn('[Kleenest] auth refresh failed',e);return null;}}
 function ensureButton(){const nav=document.querySelector('header nav');if(!nav||nav.querySelector('[data-auth-open]'))return;const b=document.createElement('button');b.dataset.authOpen='signin';b.textContent='Sign In / Sign Up';b.className='auth-button';nav.appendChild(b);}
 function gateCommunity(){const profile=window.KleenestRuntime?.user,tier=profile?.subscriptionTier||'free',premium=['premium','family','fleet','enterprise'].includes(tier),view=document.querySelector('header nav button.active')?.textContent;if(view!=='Social')return;document.querySelectorAll('button').forEach(b=>{const t=(b.textContent||'').trim();if(/^View contests$/i.test(t)||/^Start a game$/i.test(t)){const game=/Start a game/i.test(t);b.disabled=!game&&!premium;if(!game&&!premium){b.textContent='Premium required to participate';b.title='Premium membership is required to participate in contests.';}}});}
 async function init(){await refresh();ensureButton();gateCommunity();new MutationObserver(()=>{ensureButton();gateCommunity();}).observe(document.body,{childList:true,subtree:true});}
 window.KleenestAuthBridge={refresh};window.addEventListener('kleenest:auth-state-changed',refresh);document.addEventListener('click',async e=>{const b=e.target.closest('#login');if(b){e.preventDefault();window.KleenestAuthUI?.open('signin');return;}const a=e.target.closest('[data-auth-open]');if(a){e.preventDefault();window.KleenestAuthUI?.open(a.dataset.authOpen||'signin');return;}});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
