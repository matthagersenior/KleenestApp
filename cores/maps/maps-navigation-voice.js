/* Maps voice layer: uses browser speech synthesis only for real maneuver state. */
export function createMapsNavigationVoice({enabled=true,rate=1}={}){let active=enabled;function setEnabled(value){active=!!value}function speak(text){if(!active||!text||typeof window==='undefined'||!window.speechSynthesis)return false;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(String(text));u.rate=rate;u.lang='en-US';window.speechSynthesis.speak(u);return true}function stop(){window.speechSynthesis?.cancel?.()}return Object.freeze({speak,setEnabled,isEnabled:()=>active,stop})}