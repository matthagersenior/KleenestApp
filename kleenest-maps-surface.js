/* Authoritative modular Maps surface. Uses existing discovery/location modules without importing the monolith. */
(function(){'use strict';
 const M=window.KleenestMapsSurface=window.KleenestMapsSurface||{};
 const esc=v=>String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]));
 const rows=()=>window.KleenestData?.restrooms||window.KleenestState?.restrooms||[];
 let rootRef=null,filter='all';
 function renderList(){if(!rootRef)return;const list=rootRef.querySelector('#map-location-list'),status=rootRef.querySelector('#maps-status');let data=rows();if(filter!=='all')data=data.filter(x=>String(x.segment||x.placeType||'toilets').toLowerCase()===filter);list.innerHTML=data.length?data.slice(0,100).map(x=>'<article class="feed-item"><div><strong>'+esc(x.name||'Location')+'</strong><div class="muted">'+esc(x.address||x.city||x.description||'Nearby location')+'</div></div><span class="badge">'+esc(x.segment||x.placeType||'Bathroom')+'</span></article>').join(''):'<p class="muted">No locations are loaded yet. Use Refresh or enable location services.</p>';if(status&&data.length)status.textContent=data.length+' locations available';}
 async function discover(reason){const status=rootRef?.querySelector('#maps-status');try{if(status)status.textContent='Loading nearby locations…';if(window.KleenestMapDiscovery?.load){const r=await window.KleenestMapDiscovery.load(reason||'maps-surface');if(r?.status==='waiting_for_location'){if(status)status.textContent='Enable location or use Refresh to load locations.';}else if(r?.status==='error'){if(status)status.textContent=r.error?.message||'Unable to load locations.';}else if(status)status.textContent=(r?.locations?.length||0)+' locations available';}renderList();}catch(e){if(status)status.textContent=e.message||'Unable to load locations.';renderList();}}
 M.mount=async function(root){if(!root)return;rootRef=root;root.innerHTML='<section class="stack"><div class="card accent"><h1>Nearby locations</h1><p class="muted" id="maps-status">Loading locations…</p><div class="actions"><button type="button" class="primary" data-map-location>Use My Location</button><button type="button" class="secondary" data-map-refresh>Refresh</button></div></div><div class="card"><h3>Location categories</h3><div class="tabs-row" data-map-cats><button type="button" class="tab-button active" data-map-cat="all">All</button><button type="button" class="tab-button" data-map-cat="toilets">Bathrooms</button><button type="button" class="tab-button" data-map-cat="restaurant">Restaurants</button><button type="button" class="tab-button" data-map-cat="cafe">Cafes</button><button type="button" class="tab-button" data-map-cat="retail">Retail</button></div></div><div class="card"><h3>Locations</h3><div id="map-location-list"></div></div></section>';
  root.querySelector('[data-map-location]').onclick=async()=>{const s=root.querySelector('#maps-status');try{s.textContent='Requesting location permission…';await window.KleenestLocation?.getCurrentPosition?.();await discover('location-button');}catch(e){s.textContent=e.message||'Location permission was not available.';renderList();}};
  root.querySelector('[data-map-refresh]').onclick=()=>discover('manual-refresh');
  root.querySelectorAll('[data-map-cat]').forEach(b=>b.onclick=()=>{filter=b.dataset.mapCat;root.querySelectorAll('[data-map-cat]').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderList();});
  window.addEventListener('kleenest:map-discovery-ready',renderList);
  renderList();
  await discover('maps-surface');
 };
})();