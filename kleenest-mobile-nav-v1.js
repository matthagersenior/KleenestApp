/* Kleenest Mobile Navigation v2 — disabled. The modular shell owns the single primary navigation. */
(function(g){'use strict';if(g.KleenestMobileNavV1)return;function render(){}function sync(){}g.KleenestMobileNavV1={render:render,sync:sync}})(window);