/* Maps Renderer contract. The provider implementation is injected; Maps Core never owns provider-specific global state. */
export function createMapsRenderer({ provider=null }={}) { let mountRoot=null; async function mount(root,context){mountRoot=root;if(provider?.mount)return provider.mount(root,context);root.innerHTML='<div class="maps-provider-placeholder"><strong>Map surface</strong><span>Map provider is ready to be connected.</span></div>';} async function refresh(context){return provider?.refresh?.(context)} function destroy(){provider?.destroy?.();mountRoot=null} return Object.freeze({mount,refresh,destroy}); }