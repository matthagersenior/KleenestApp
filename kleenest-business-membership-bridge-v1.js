/* Kleenest business membership bridge — resolves memberships independently of nested Supabase relationships. */
(function(g){'use strict';if(g.KleenestBusinessMembershipBridgeV1)return;var original=g.KleenestSupabase?.businessMemberships;async function businessMemberships(){var s=await g.KleenestSupabase.session();if(!s)return[];var c=g.KleenestSupabase.client();var q=await c.from('business_members').select('business_id,user_id,role,created_at').eq('user_id',s.user.id);var memberships=!q.error&&Array.isArray(q.data)?q.data:[];if(!memberships.length){var a=await c.from('app_business_memberships').select('business_id,user_id,role,created_at,business_name').eq('user_id',s.user.id);if(!a.error&&Array.isArray(a.data))memberships=a.data}if(memberships.length){var ids=[...new Set(memberships.map(function(x){return x.business_id}).filter(Boolean))];var b=await c.from('businesses').select('*').in('id',ids);var byId=new Map((b.data||[]).map(function(x){return[String(x.id),x]}));return memberships.map(function(m){var biz=byId.get(String(m.business_id))||{};return Object.assign({},m,{businesses:biz,business:biz})})}return original?original():[]}
g.KleenestSupabase.businessMemberships=businessMemberships;g.KleenestBusinessMembershipBridgeV1={resolve:businessMemberships};})(window);