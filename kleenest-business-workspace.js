/* Kleenest Business Workspace — canonical business operating surface. */
(function(){'use strict';
const W=window.KleenestBusinessWorkspace=window.KleenestBusinessWorkspace||{};let db,business,membership,view='overview',locationId='all',rows=[],locations=[],loading=false,error='';
const DATA={overview:['Overview','businesses'],locations:['Locations','locations'],qr:['QR Studio','qr_codes'],promotions:['Promotions','promotions'],campaigns:['Campaigns','business_campaigns'],events:['Events','business_events'],contests:['Contests','contests'],rewards:['Rewards & Clubs','membership_clubs'],partnerships:['Partnerships','partner_programs'],reviews:['Reviews','reviews'],media:['Media','location_photos'],analytics:['Analytics & ROI','analytics_events'],verification:['Verification','business_certifications']};
const STANDARD=new Set(['overview','locations','reviews','verification']);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const client=()=>window.KleenestSupabase?.client?.()||window.KleenestRuntime?.supabase;
function tier(){return String(business?.business_tier||'standard').toLowerCase()}function can(k){return tier()!=='standard'||STANDARD.has(k)||['owner','admin'].includes(String(membership?.role||'').toLowerCase())}function canWrite(){return ['owner','admin','manager'].includes(String(membership?.role||'').toLowerCase())}function assertWrite(){if(!canWrite())throw Error('Your business role does not have write permission.');}
async function loadContext(){db=client();if(!db)throw Error('Supabase client unavailable.');const u=(await db.auth.getUser()).data?.user||window.KleenestRuntime?.user;if(!u)throw Error('Sign in required.');const r=await db.from('business_members').select('role,business_id,businesses(*)').eq('user_id',u.id);if(r.error)throw r.error;membership=(r.data||[]).find(x=>['owner','admin','manager','analyst','staff'].includes(String(x.role).toLowerCase()))||null;business=membership?.businesses||null;if(!business)throw Error('No business membership found.');const l=await db.from('locations').select('id,name,is_active').eq('business_id',business.id).order('name');if(l.error)throw l.error;locations=l.data||[];window.__kleenestBusinessLocations=locations;}
async function getRows(table){let q=db.from(table).select('*');if(['promotions','business_campaigns','business_events','contests','membership_clubs','partner_programs','business_certifications'].includes(table))q=q.eq('business_id',business.id);if(table==='locations')q=q.eq('business_id',business.id);if(['qr_codes','reviews','location_photos'].includes(table)&&locationId!=='all')q=q.eq('location_id',locationId);if(table==='analytics_events')q=q.eq('business_id',business.id);const r=await q.order('created_at',{ascending:false}).limit(100);if(r.error)throw r.error;return r.data||[];}
async function getMetrics(){const ids=locations.map(x=>x.id);const [a,c,r,p,q]=await Promise.all([db.from('analytics_events').select('event_type').eq('business_id',business.id),db.from('check_ins').select('id').in('location_id',ids.length?ids:['00000000-0000-0000-0000-000000000000']),db.from('reviews').select('stars').in('location_id',ids.length?ids:['00000000-0000-0000-0000-000000000000']),db.from('promotions').select('id,active').eq('business_id',business.id),db.from('qr_codes').select('id,active').in('location_id',ids.length?ids:['00000000-0000-0000-0000-000000000000'])]);const ev=a.data||[],rv=r.data||[];return{views:ev.filter(x=>['location_view','location_impression'].includes(x.event_type)).length,qr:ev.filter(x=>x.event_type==='qr_scan').length,checkins:(c.data||[]).length,reviews:rv.length,rating:rv.length?rv.reduce((s,x)=>s+Number(x.stars||0),0)/rv.length:0,promos:(p.data||[]).filter(x=>x.active).length,qrs:(q.data||[]).filter(x=>x.active).length};}
async function load(){loading=true;error='';try{await loadContext();if(!can(view))view='overview';rows=view==='overview'?await getMetrics():await getRows(DATA[view][1]);}catch(e){error=e.message||String(e);rows=[];}finally{loading=false;}}
function nav(){return Object.entries(DATA).map(([k,v])=>`<button class="biz-tab ${view===k?'active':''} ${can(k)?'':'locked'}" data-biz-view="${k}">${esc(v[0])}${can(k)?'':' 🔒'}</button>`).join('');}
function selector(){return `<div class="biz-controls"><label>Location<select id="biz-location"><option value="all">All locations</option>${locations.map(x=>`<option value="${esc(x.id)}" ${locationId===x.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select></label><button data-biz-refresh>↻ Refresh</button></div>`;}
function overview(){const m=rows||{};return `<div class="biz-metrics"><article><span>Discovery views</span><b>${m.views||0}</b><small>views + impressions</small></article><article><span>QR scans</span><b>${m.qr||0}</b><small>attributable scans</small></article><article><span>Check-ins</span><b>${m.checkins||0}</b><small>consumer visits</small></article><article><span>Reviews</span><b>${m.reviews||0}</b><small>${Number(m.rating||0).toFixed(1)} ★ average</small></article><article><span>Active promotions</span><b>${m.promos||0}</b><small>offers in market</small></article><article><span>Active QR assets</span><b>${m.qrs||0}</b><small>scannable experiences</small></article></div><div class="biz-feature-grid"><article><b>📷 QR STUDIO</b><h3>Every scan can become a conversion.</h3><p>Branded QR assets can trigger check-ins, promotions, campaigns, events or custom actions.</p><button data-biz-view="qr">Open Studio</button></article><article><b>🎯 GROWTH</b><h3>Build measurable engagement loops.</h3><p>Promotions, campaigns, contests, events and rewards connect consumer actions to analytics.</p><button data-biz-view="campaigns">Open Growth</button></article><article><b>🏆 PROGRESSION</b><h3>Make your location part of the game.</h3><p>Check-ins, QR scans, reviews, redemptions, routes and contests can feed shared progression.</p><button data-biz-view="contests">Open Contests</button></article></div>`;}
function qr(){return `<div class="biz-toolbar"><button data-create="qr">＋ New QR Experience</button><span>Customize purpose, action, destination, branding and engagement.</span></div><div class="biz-grid">${rows.length?rows.map(r=>`<article class="biz-card"><div class="qr-art" style="--a:${esc(r.customization?.accent||'#0e7c69')}"><strong>▦</strong><span>${esc(r.label||r.code)}</span></div><h3>${esc(r.label||r.code)}</h3><p>${esc(r.purpose||'checkin')} · ${esc(r.action_type||'checkin')} · ${r.active?'Active':'Paused'}</p><div class="biz-row"><button data-qr-edit="${r.id}">Customize</button><button data-qr-toggle="${r.id}">${r.active?'Pause':'Activate'}</button></div></article>`).join(''):'<div class="biz-empty"><h3>No QR experiences yet</h3><p>Create your first branded check-in, promotion, event or custom action.</p><button data-create="qr">Create QR</button></div>'}</div>`;}
function records(){if(!rows.length)return `<div class="biz-empty"><h3>No ${esc(DATA[view][0].toLowerCase())} yet.</h3>${canWrite()?`<button data-create="record">Create ${esc(DATA[view][0])}</button>`:'<p>This dataset is visible when records exist or when your business role permits creation.</p>'}</div>`;return `<div class="biz-grid">${rows.map(r=>{const title=r.name||r.title||r.label||r.code||'Record';const vals=Object.entries(r).filter(([k,v])=>v!=null&&v!==''&&!['id','business_id'].includes(k)).slice(0,7);return `<article class="biz-card"><small>${esc(r.status||r.active===false?'Paused':'Active')}</small><h3>${esc(title)}</h3>${vals.map(([k,v])=>`<div class="biz-line"><span>${esc(k.replace(/_/g,' '))}</span><strong>${esc(typeof v==='object'?JSON.stringify(v):v)}</strong></div>`).join('')}${['promotions','business_campaigns','business_events','contests','membership_clubs','partner_programs'].includes(DATA[view][1])&&canWrite()?`<div class="biz-row"><button data-record-edit="${r.id}">Edit</button><button data-record-delete="${r.id}">Delete</button></div>`:''}</article>`;}).join('')}</div>`;}
function style(){if(document.getElementById('biz-final-style'))return;const s=document.createElement('style');s.id='biz-final-style';s.textContent='.biz{--ink:#123b32;--muted:#647870;--line:#d9e8e1;--teal:#0d7d69;color:var(--ink)}.biz *{box-sizing:border-box}.biz-hero{padding:28px;border-radius:26px;background:linear-gradient(135deg,#063f35,#0e806d 58%,#69d4b4);color:white;box-shadow:0 18px 44px #123d321c}.biz-hero h1{margin:5px 0;font-size:clamp(2rem,5vw,3.1rem)}.biz-hero p{color:#e1f5ef}.biz-nav{display:flex;gap:7px;overflow:auto;padding:14px 0}.biz-tab,.biz button,.biz select{border:1px solid var(--line);background:white;border-radius:12px;padding:10px 13px;font-weight:850}.biz-tab{white-space:nowrap}.biz-tab.active,.biz-feature button,.biz-toolbar button{background:var(--teal);color:white;border-color:var(--teal)}.biz-tab.locked{opacity:.65}.biz-controls{display:flex;justify-content:space-between;gap:12px;align-items:end;background:#fff;border:1px solid var(--line);padding:14px;border-radius:16px;margin-bottom:14px}.biz-controls label{display:grid;gap:5px;font-weight:800}.biz-metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.biz-metrics article,.biz-card,.biz-feature{background:#fff;border:1px solid var(--line);border-radius:20px;padding:19px;box-shadow:0 8px 28px #16352d0a}.biz-metrics span,.biz-metrics small{display:block;color:var(--muted)}.biz-metrics b{display:block;font-size:2rem;margin:5px 0}.biz-feature-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin-top:14px}.biz-feature{background:linear-gradient(145deg,#fff,#eff8f4)}.biz-feature h3{margin:7px 0}.biz-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.biz-card small{color:var(--muted);font-weight:800}.biz-line{display:flex;justify-content:space-between;gap:10px;border-top:1px solid #edf3f0;padding:8px 0}.biz-line span{color:var(--muted);text-transform:capitalize}.biz-row{display:flex;gap:7px;margin-top:12px}.biz-toolbar{display:flex;align-items:center;gap:12px;margin-bottom:14px}.biz-toolbar span{color:var(--muted)}.qr-art{height:130px;border-radius:16px;background:linear-gradient(135deg,var(--a),#143b32);color:#fff;display:flex;align-items:center;justify-content:space-between;padding:18px;margin-bottom:12px}.qr-art strong{font-size:4.5rem}.biz-empty{text-align:center;padding:44px;background:#fff;border:1px dashed var(--line);border-radius:20px;grid-column:1/-1}.biz-message{padding:13px;background:#fff0f0;color:#9a3030;border-radius:12px;margin-bottom:14px}@media(max-width:800px){.biz-grid,.biz-metrics,.biz-feature-grid{grid-template-columns:1fr}.biz-controls,.biz-toolbar{display:block}.biz-controls>*{margin-top:7px}}';document.head.appendChild(s)}
async function createQR(){assertWrite();const loc=locationId==='all'?(await db.from('locations').select('id').eq('business_id',business.id).limit(1)).data?.[0]?.id:locationId;if(!loc)throw Error('This business needs at least one location before creating a QR experience.');const label=prompt('QR experience name','Kleenest Check-In');if(!label)return;const purpose=prompt('Purpose: checkin, promotion, campaign, event, custom','checkin');const action=prompt('Action type: checkin, promotion, campaign, event, url, custom','checkin');const payloadText=prompt('Action payload JSON (optional)','{}');let action_payload={};try{action_payload=JSON.parse(payloadText||'{}')}catch(e){throw Error('Action payload must be valid JSON.')}const r=await db.from('qr_codes').insert({location_id:loc,code:'KN-'+crypto.randomUUID().slice(0,8).toUpperCase(),label,purpose,action_type:action,action_payload,active:true,customization:{accent:'#0d7d69',shape:'rounded',logo:'kleenest',show_label:true}});if(r.error)throw r.error;await load();render(document.getElementById('business-workspace'))}
async function editQR(id){assertWrite();const r=await db.from('qr_codes').select('*').eq('id',id).single();if(r.error)throw r.error;const d=r.data;const label=prompt('Label',d.label||'');if(label==null)return;const purpose=prompt('Purpose',d.purpose||'checkin');const action=prompt('Action type',d.action_type||'checkin');const payload=prompt('Action payload JSON',JSON.stringify(d.action_payload||{}));let p;try{p=JSON.parse(payload||'{}')}catch(e){throw Error('Action payload must be valid JSON.')}const accent=prompt('Brand accent color',d.customization?.accent||'#0d7d69');const u=await db.from('qr_codes').update({label,purpose,action_type:action,action_payload:p,customization:{...(d.customization||{}),accent}}).eq('id',id);if(u.error)throw u.error;await load();render(document.getElementById('business-workspace'))}
async function toggleQR(id){assertWrite();const q=await db.from('qr_codes').select('active').eq('id',id).single();if(q.error)throw q.error;const u=await db.from('qr_codes').update({active:!q.data.active}).eq('id',id);if(u.error)throw u.error;await load();render(document.getElementById('business-workspace'))}
async function createRecord(){assertWrite();const table=DATA[view][1];const title=prompt(`Name/title for new ${DATA[view][0]}`);if(!title)return;let p={};if(table==='promotions')p={business_id:business.id,title,description:'',active:true};else if(table==='business_campaigns')p={business_id:business.id,name:title,description:'',status:'draft'};else if(table==='business_events')p={business_id:business.id,title,description:'',status:'draft',metrics_config:{rsvp:true,attendance:true,check_ins:true,qr_scans:true}};else if(table==='contests')p={business_id:business.id,name:title,description:'',status:'draft',scoring_rules:{check_ins:10,reviews:15,qr_scans:10,event_attend:20},rewards:{points:100},metrics_config:{entries:true,engagement:true,redemptions:true}};else if(table==='membership_clubs')p={business_id:business.id,name:title,active:true,fee_cents:0,perks:''};else if(table==='partner_programs')p={business_id:business.id,name:title,enabled:false,preferred_access:false,match_discount_bonus:0,custom_perk:''};else throw Error('This dataset has no creation workflow yet.');const r=await db.from(table).insert(p);if(r.error)throw r.error;await load();render(document.getElementById('business-workspace'))}
async function deleteRecord(id){assertWrite();if(!confirm('Delete this record?'))return;const table=DATA[view][1];const r=await db.from(table).delete().eq('id',id).eq('business_id',business.id);if(r.error)throw r.error;await load();render(document.getElementById('business-workspace'))}
async function editRecord(id){assertWrite();const table=DATA[view][1];const r=await db.from(table).select('*').eq('id',id).eq('business_id',business.id).single();if(r.error)throw r.error;const d=r.data;const nameField=['promotions','business_events'].includes(table)?'title':['business_campaigns','contests','membership_clubs','partner_programs'].includes(table)?'name':null;if(!nameField)return;const title=prompt('Name/title',d[nameField]||'');if(title==null)return;const desc=prompt('Description',d.description||'');const status=prompt('Status',d.status||'active');const u=await db.from(table).update({[nameField]:title,description:desc,status}).eq('id',id).eq('business_id',business.id);if(u.error)throw u.error;await load();render(document.getElementById('business-workspace'))}
function render(root){style();if(!root)return;root.innerHTML=`<div class="biz-hero"><small>BUSINESS STUDIO · ${esc(tier()).toUpperCase()}</small><h1>${esc(business?.name||'Business')}</h1><p>Operate locations, build engagement, measure outcomes and turn Kleenest activity into business value.</p></div><nav class="biz-nav">${nav()}</nav>${error?`<div class="biz-message">${esc(error)}</div>`:''}${loading?'<div class="biz-empty"><h3>Loading business data…</h3></div>':`<section>${selector()}${view==='overview'?overview():view==='qr'?qr():records()}</section>`}`;root.querySelectorAll('[data-biz-view]').forEach(b=>b.onclick=async()=>{const k=b.dataset.bizView;if(!can(k)){alert(`${DATA[k][0]} is not included in the ${tier()} business tier.`);return;}view=k;await load();render(root)});root.querySelector('#biz-location')?.addEventListener('change',async e=>{locationId=e.target.value;await load();render(root)});root.querySelector('[data-biz-refresh]')?.addEventListener('click',async()=>{await load();render(root)});root.querySelectorAll('[data-create]').forEach(b=>b.onclick=()=>{(b.dataset.create==='qr'?createQR():createRecord()).catch(e=>alert(e.message))});root.querySelectorAll('[data-qr-edit]').forEach(b=>b.onclick=()=>editQR(b.dataset.qrEdit).catch(e=>alert(e.message)));root.querySelectorAll('[data-qr-toggle]').forEach(b=>b.onclick=()=>toggleQR(b.dataset.qrToggle).catch(e=>alert(e.message)));root.querySelectorAll('[data-record-delete]').forEach(b=>b.onclick=()=>deleteRecord(b.dataset.recordDelete).catch(e=>alert(e.message)));root.querySelectorAll('[data-record-edit]').forEach(b=>b.onclick=()=>editRecord(b.dataset.recordEdit).catch(e=>alert(e.message)));}
W.mount=async root=>{await load();render(root)};W.load=load;W.render=render;W.allowed=()=>!!business;W.getContext=()=>({business,membership,view,locationId,rows,locations});})();