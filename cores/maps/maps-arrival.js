/* Maps arrival: real stop/destination arrival state derived from GPS and route stops. */
export function createMapsArrival({radiusMeters=75,onChange=()=>{}}={}){let destination=null,arrived=false;function setDestination(point){destination=point?{latitude:Number(point.latitude),longitude:Number(point.longitude)}:null;arrived=false}function update(position){if(!destination||!position?.coords)return{arrived:false,distanceMeters:null};const d=haversine({latitude:position.coords.latitude,longitude:position.coords.longitude},destination);if(!arrived&&d<=radiusMeters){arrived=true;onChange({arrived:true,distanceMeters:Math.round(d)})}return{arrived,distanceMeters:Math.round(d)}}return Object.freeze({setDestination,update,isArrived:()=>arrived})}function haversine(a,b){const R=6371000,la=a.latitude*Math.PI/180,lb=b.latitude*Math.PI/180,dla=(b.latitude-a.latitude)*Math.PI/180,dl=(b.longitude-a.longitude)*Math.PI/180,x=Math.sin(dla/2)**2+Math.cos(la)*Math.cos(lb)*Math.sin(dl/2)**2;return 2*R*Math.atan2(Math.sqrt(x),Math.sqrt(1-x))}