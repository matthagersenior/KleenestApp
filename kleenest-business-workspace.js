/* Full Business / Enterprise workspace. Dataset and location selection are authoritative; management actions are admin-only. */
(function(){'use strict';
 const W=window.KleenestBusinessWorkspace=window.KleenestBusinessWorkspace||{};
 const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
 let business=null,membership=null,dataset='overview',locationId='all',locations=[],model=null,locationOverviews=[],error='';
 const datasets=[['overview','Overview'],['locations','Locations'],['engagement','Engagement'],['qr','QR & Check-ins'],['visitors','Visitors & Retention'],['reviews','Reviews & Reputation'],['photos','Photos & Media'],['promotions','Promotions & Offers'],['campaigns','Campaigns'],['partnerships','Partnerships'],['rewards','Rewards & Redemptions'],['events','Events'],['occupancy','Occupancy'],['roi','ROI'],['growth','Growth & Intelligence'],['benchmarks','Benchmarks'],['verification','Verification & Tier']];
 const unavailable=['roi'];
 async function memberships(){try{return await window.KleenestSupabase.businessMemberships?.()||[]}catch(e){console.warn('Business membership lookup failed; using dashboard fallback.',e);return[]}}
 async function locationOverview(id){try{return await window.kleenestBusinessAnalytics?.get?.(business.id,null,null,id)||[]}catch(e){return[]}}
 async function load(){
  error='';
  const ms=await memberships(); membership=ms.find(x=>['owner','admin'].includes(String(x.role||'').toLowerCase()))||ms.find(Boolean)||null; business=membership?.businesses||null;
  if(!business){try{const d=await window.KleenestBusinessSurface?.getDashboard?.();business=d?.business||d;}catch(e){}}
  if(!business){try{const d=await window.KleenestSupabase?.rpc?.('get_business_dashboard');business=d?.business||d;membership=membership||{role:'owner'};}catch(e){business=null;error=e.message||'Business unavailable';}}
  if(business){try{
    const ld=await window.kleenestBusinessAnalytics?.details?.(business.id,'locations'); let lr=ld?.rows||[];
    if(!lr.length){const la=await window.kleenestBusinessAnalytics?.dataset?.(business.id,'locations');lr=Array.isArray(la?.data?.rows)?la.data.rows:[];}
    locations=lr.map(r=>({id:r.id||r.location_id,name:r.name||r.location_name||r.title||'Unnamed location',active:r.is_active??r.active})).filter(x=>x.id);
    if(locationId!=='all'&&!locations.some(x=>x.id===locationId))locationId='all';
    const all=await window.kleenestBusinessAnalytics?.get?.(business.id,null,null,locationId==='all'?null:locationId)||[];
    model={analytics:all.map(x=>({...x,key:x.key,dataset:x.dataset,fields:x.fields,error:x.error}))};
    if(locationId==='all'&&dataset==='overview'){
      const results=await Promise.all(locations.map(async l=>({location:l,analytics:await locationOverview(l.id)})));
      locationOverviews=results.map(x=>({...x,overview:(x.analytics||[]).find(a=>a.key==='overview')?.dataset||{}}));
    }else locationOverviews=[];
  }catch(e){error=e.message||'Analytics unavailable';model={analytics:[]};locationOverviews=[];}}
  return{business,membership,model,locations};
 }
 function isAdmin(){const u=window.KleenestRuntime?.user;return !!(u?.isAdmin||['owner','admin'].includes(String(membership?.role||'').toLowerCase())||String(u?.email||'').toLowerCase()==='matthagersr@gmail.com');}
 function allowed(){const u=window.KleenestRuntime?.user;return !!(u?.isAdmin||business||['enterprise'].includes(String(u?.subscriptionTier||'').toLowerCase())||String(u?.email||'').toLowerCase()==='matthagersr@gmail.com');}
 const fmt=(v,key)=>{if(v==null||v==='')return '—';if(key==='conversion_rate_pct'||key==='retention_rate_pct'||key==='cleanliness_pct'||key==='percentile'||key==='roi')return `${Number(v).toLocaleString(undefined,{maximumFractionDigits:2})}%`;if(key==='storage_bytes'||key==='average_size_bytes')return `${Number(v).toLocaleString()} bytes`;if(key==='peak_hour')return `${String(v).padStart(2,'0')}:00`;if(typeof v==='number')return new Intl.NumberFormat().format(v);return String(v)};
 const label=k=>String(k).replace(/_/g,' ').replace(/\b\w/g,m=>m.toUpperCase());
 function metric(key,value){return `<div class="stat"><b>${esc(fmt(value,key))}</b><span>${esc(label(key))}</span></div>`;}
 function prettyValue(k,v){if(v==null||v==='')return '—';if(typeof v==='object')return JSON.stringify(v,null,2);return fmt(v,k)}
 function detailCards(rows){if(!rows?.length)return '<div class="empty">No records are available for this selection.</div>';return `<div class="detail-grid" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:18px;margin-top:12px">${rows.map((r,i)=>{const entries=Object.entries(r).filter(([k,v])=>v!==null&&v!==undefined&&v!=='').slice(0,12);const heading=r.title||r.name||r.label||`${label(dataset)} #${i+1}`;return `<article class="detail-card" style="display:block;box-sizing:border-box;min-width:0;padding:20px;border:1px solid rgba(127,127,127,.22);border-radius:16px;background:rgba(127,127,127,.05);box-shadow:0 2px 8px rgba(0,0,0,.05);overflow:hidden"><h4 style="margin:0 0 16px;font-size:1.08rem;line-height:1.3;overflow-wrap:anywhere">${esc(heading)}</h4><div class="detail-fields" style="display:flex;flex-direction:column;gap:0;width:100%">${entries.map(([k,v])=>`<div class="detail-field" style="display:block;width:100%;box-sizing:border-box;padding:11px 0;border-top:1px solid rgba(127,127,127,.16)"><div class="detail-label" style="display:block;width:100%;margin:0 0 5px;font-size:.72rem;font-weight:800;line-height:1.2;letter-spacing:.06em;text-transform:uppercase;opacity:.62">${esc(label(k))}</div><div class="detail-value" style="display:block;width:100%;margin:0;font-size:1rem;font-weight:650;line-height:1.45;white-space:normal;overflow-wrap:anywhere;word-break:break-word">${esc(prettyValue(k,v))}</div></div>`).join('')}</div></article>`}).join('')}</div>`;}
 function overviewByLocation(){if(!locationOverviews.length)return '';return `<div class="card" style="margin-top:18px"><div class="title-row"><div><h3>Location Overview</h3><p class="muted">Individual performance for every location. Business totals are shown above.</p></div></div><div class="detail-grid" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:18px;margin-top:12px">${locationOverviews.map(({location,overview})=>{const keys=['locations','check_ins','reviews','average_rating','cleanliness_pct','qr_scans','visitors','promotions','promotion_redemptions','reward_points'];const available=keys.filter(k=>overview[k]!==undefined);return `<article class="detail-card" style="padding:20px;border:1px solid rgba(127,127,127,.22);border-radius:16px;background:rgba(127,127,127,.05)"><h4 style="margin:0 0 14px">${esc(location.name)}</h4><div class="stats">${available.length?available.map(k=>metric(k,overview[k])).join(''):'<div class="empty">No location metrics available.</div>'}</div></article>`}).join('')}</div></div>`;}
 function actions(){if(!isAdmin())return '';if(dataset==='locations')return `<div class="card"><h3>Manage locations</h3><button class="primary" data-biz-action="new-location">+ Add location</button></div>`;if(dataset==='qr')return `<div class="card"><h3>Manage QR</h3><p class="muted">Create QR codes tied to a business location.</p><button class="primary" data-biz-action="new-qr">+ Create QR</button></div>`;if(dataset==='promotions')return `<div class="card"><h3>Manage promotions</h3><button class="primary" data-biz-action="new-promotion">+ Create promotion</button></div>`;if(dataset==='campaigns')return `<div class="card"><h3>Manage campaigns</h3><p class="muted">Campaign creation is restricted to business admins.</p><button class="primary" data-biz-action="new-campaign">+ Create campaign</button></div>`;if(dataset==='partnerships')return `<div class="card"><h3>Manage partnerships</h3><button class="primary" data-biz-action="new-partner">+ Create partner program</button></div>`;if(dataset==='events')return `<div class="card"><h3>Manage events</h3><button class="primary" data-biz-action="new-event">+ Create event</button></div>`;return '';}
 async function action(kind){if(!isAdmin())throw Error('Business admin permission required');if(kind==='new-location'){const name=prompt('Location name');if(!name)return;await window.kleenestBusinessAnalytics.rpc('business_create_location',{p_business_id:business.id,p_name:name});}if(kind==='new-promotion'){const title=prompt('Promotion title');if(!title)return;await window.kleenestBusinessAnalytics.rpc('business_create_promotion',{p_business_id:business.id,p_title:title});}if(kind==='new-event'){const title=prompt('Event title');if(!title)return;await window.kleenestBusinessAnalytics.rpc('business_create_event',{p_business_id:business.id,p_title:title});}if(kind==='new-partner'){const name=prompt('Partner program name');if(!name)return;await window.kleenestBusinessAnalytics.rpc('business_create_partner_program',{p_business_id:business.id,p_name:name});}if(kind==='new-qr'){const id=locationId==='all'?prompt('Location ID for this QR'):locationId;if(!id)return;const label=prompt('QR label')||'Business QR';await window.kleenestBusinessAnalytics.rpc('business_create_qr',{p_business_id:business.id,p_location_id:id,p_label:label});}if(kind==='new-campaign')throw Error('Campaign creation requires an enterprise campaign network; this control will not create an unscoped campaign.');await load();}
 function body(){if(dataset==='verification')return `<div class="card"><h3>Verification & tier</h3><div class="stats">${metric('business_tier',business?.business_tier||'standard')}${metric('verification',business?.verification_status||'pending')}${metric('member_role',membership?.role||'staff')}${metric('admin',isAdmin()?'Authorized':'No')}</div></div>`;const row=(model?.analytics||[]).find(x=>x.key===dataset);const title=datasets.find(x=>x[0]===dataset)?.[1]||dataset;if(unavailable.includes(dataset))return `<div class="card"><h3>${esc(title)}</h3><div class="message-banner">Revenue attribution is not configured. ROI is intentionally not calculated until a verified revenue source is available.</div></div>`;if(!row)return `<div class="card"><h3>${esc(title)}</h3><div class="message-banner error">No dataset result was returned.</div><button class="primary" data-biz-action="refresh">Retry</button></div>`;if(row.error)return `<div class="card"><h3>${esc(title)}</h3><div class="message-banner error">${esc(row.error)}</div><button class="primary" data-biz-action="refresh">Retry</button></div>`;const d=row.dataset||{};const fields=row.fields||window.kleenestBusinessAnalytics?.fields?.[dataset]||Object.keys(d);const selected=locationId==='all'?'All Locations':(locations.find(l=>l.id===locationId)?.name||'Selected Location');const records=Array.isArray(d.rows)?d.rows:[];return `<div class="card"><div class="title-row"><div><h3>${esc(title)}</h3><p class="muted">${esc(selected)} · ${locationId==='all'?'business-wide':'location-specific'} calculation</p></div></div><div class="stats" style="margin-top:12px">${fields.map(f=>metric(f,d[f])).join('')}</div>${records.length?`<h3 style="margin-top:20px">${esc(title)} details</h3>${detailCards(records)}`:''}</div>${dataset==='overview'&&locationId==='all'?overviewByLocation():''}${actions()}`;}
 function render(root){if(!root)return;if(!allowed()){root.innerHTML='<div class="card"><h1>Business</h1><p class="muted">Business access is available to business members.</p></div>';return;}root.innerHTML=`<section class="stack"><div class="title-row"><div><h1>🏢 ${esc(business?.name||'Business')}</h1><p class="muted">${esc(business?.business_tier||'standard').toUpperCase()} · ${esc(membership?.role||'owner')}</p></div><button class="ghost" data-biz-action="refresh">↻ Refresh</button></div>${error?`<div class="message-banner error">${esc(error)}</div>`:''}<div class="card"><label style="font-weight:800">Location</label><select id="business-location" class="input"><option value="all" ${locationId==='all'?'selected':''}>All Locations (${locations.length})</option>${locations.map(l=>`<option value="${esc(l.id)}" ${locationId===l.id?'selected':''}>${esc(l.name)}${l.active===false?' (Inactive)':''}</option>`).join('')}</select><label style="font-weight:800;display:block;margin-top:14px">Analytics dataset</label><select id="business-dataset" class="input">${datasets.map(([k,v])=>`<option value="${k}" ${dataset===k?'selected':''}>${v}</option>`).join('')}</select><p class="muted">Choose a location and dataset. The displayed metrics and records are recalculated for that selection.</p></div>${body()}</section>`;root.querySelector('#business-dataset').onchange=async e=>{dataset=e.target.value;await load();render(root)};root.querySelector('#business-location').onchange=async e=>{locationId=e.target.value;await load();render(root)};root.querySelectorAll('[data-biz-action]').forEach(b=>b.onclick=async()=>{try{if(b.dataset.bizAction==='refresh')await load();else await action(b.dataset.bizAction);render(root)}catch(e){error=e.message||String(e);render(root)}});}
 W.load=load;W.render=render;W.allowed=allowed;W.isAdmin=isAdmin;W.getContext=()=>({business,membership,model,locations,locationId});W.getLocation=id=>locations.find(x=>x.id===id);W.mount=async root=>{await load();render(root);};
})();