/* Canonical Maps Tab Core — bathroom-first production surface. */
export async function createMapsCore(options={}){const mod=await import('./maps-core.js?maps-production=v10');return mod.createMapsCore(options)}
