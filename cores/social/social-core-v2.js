/* Kleenest Social Core v2 — canonical community surface. */
import { ensureSocialStyles } from './social-theme.js';
import { createProgressionMetricsCore } from '../progression/progression-metrics-core.js';

const VIEWS = Object.freeze({
  feed: ['Feed', 'Community posts, discoveries, tips and wins.'],
  network: ['Network', 'People, follows and community connections.'],
  discover: ['Discover', 'Places, events, offers and community knowledge.'],
  compete: ['Compete', 'Challenges, contests, games and community progress.'],
  messages: ['Messages', 'Your community conversations.'],
  notifications: ['Notifications', 'Updates about your community activity.']
});

export function createSocialCore({ supabase, mediaCore, root, user = null } = {}) {
  if (!supabase) throw new Error('Social Core requires Supabase.');
  if (!root) throw new Error('Social Core requires a mount root.');
  ensureSocialStyles();
  const progression = createProgressionMetricsCore({ supabase });
  const state = { user, view: 'feed', destroyed: false, posts: [], summary: [] };
  const q = table => supabase.from(table);
  const safe = fn => Promise.resolve().then(fn).catch(error => { console.warn('[Kleenest] Social optional data unavailable', error); return []; });
  async function list(table, builder) { return safe(async () => { let query = q(table).select('*'); if (builder) query = builder(query) || query; const { data, error } = await query.limit(40); if (error) throw error; return data || []; }); }
  async function getPosts() { return safe(async () => { const { data, error } = await q('social_posts').select('*').order('created_at', { ascending: false }).limit(50); if (error) throw error; state.posts = data || []; return state.posts; }); }
  async function loadSummary() { state.summary = await safe(() => progression.summary()) || []; return state.summary; }
  async function createPost({ content, kind = 'discovery', file = null } = {}) { if (!state.user) throw new Error('Sign in required.'); if (!content?.trim()) throw new Error('Post content is required.'); let media=null; if(file){if(!mediaCore)throw new Error('Media Core is unavailable.');media=await mediaCore.upload(file,{userId:state.user.id,namespace:'social',pathPrefix:'posts'});} const payload={user_id:state.user.id,content:content.trim(),kind}; if(media)Object.assign(payload,{media_url:media.url,media_type:media.media_type,media_storage_path:media.storage_path,media_size_bytes:media.media_size_bytes}); const {data,error}=await q('social_posts').insert(payload).select().single(); if(error)throw error; await safe(()=>progression.record('social_post',{sourceId:data.id,metadata:{kind}})); return data; }
  const esc = value => String(value ?? '').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const date = value => { const d=new Date(value); return Number.isNaN(d.getTime())?'':d.toLocaleDateString(undefined,{month:'short',day:'numeric'}); };
  const empty = text => `<div class="social-core__empty-inline">${esc(text)}</div>`;
  const detail = (title,text,meta='') => `<article class="social-core__detail-card"><strong>${esc(title)}</strong>${meta?`<small>${esc(meta)}</small>`:''}<p>${esc(text)}</p></article>`;
  const metric = name => Number(state.summary.find(x=>x.metric===name)?.quantity||0);
  function progressCard(){const xp=state.summary.reduce((n,x)=>n+Number(x.points_awarded||0),0),actions=state.summary.reduce((n,x)=>n+Number(x.event_count||0),0);return `<section class="social-core__progress"><div><span>COMMUNITY XP</span><strong>${xp.toLocaleString()}</strong><small>${actions.toLocaleString()} progression actions</small></div><div class="social-core__progress-grid"><span>Posts ${metric('social_post')}</span><span>Comments ${metric('social_comment')}</span><span>Likes ${metric('social_like')}</span><span>Saves ${metric('social_save')}</span></div></section>`;}
  async function panelData(view){if(view==='network')return{follows:await list('follows',x=>state.user?x.eq('follower_id',state.user.id):x),activity:await list('social_activity',x=>x.order('created_at',{ascending:false}))};if(view==='discover')return{locations:await list('locations',x=>x.eq('is_active',true).order('updated_at',{ascending:false})),events:await list('business_events',x=>x.eq('status','active').order('event_date',{ascending:true})),promotions:await list('promotions',x=>x.eq('active',true).order('created_at',{ascending:false}))};if(view==='compete')return{games:await list('progression_games',x=>x.eq('enabled',true)),challenges:await list('progression_challenges',x=>x.eq('enabled',true)),contests:await list('contests',x=>x.in('status',['scheduled','active']))};if(view==='messages')return{messages:state.user?await list('messages',x=>x.or(`from_id.eq.${state.user.id},to_id.eq.${state.user.id}`)):[]};return{notifications:state.user?await list('notifications',x=>x.eq('user_id',state.user.id).order('created_at',{ascending:false})):[]};}
  async function panel(view){if(view==='feed')return `<div class="social-core__feed" data-region="feed"></div>`;const data=await panelData(view);if(view==='network')return `<section class="social-core__detail-grid"><div class="social-core__detail-section"><div class="social-core__section-head"><div><span>NETWORK</span><h2>Connections</h2></div><strong>${data.follows.length}</strong></div>${data.follows.map(x=>detail('Following',`Member ${x.following_id}`,date(x.created_at))).join('')||empty('You have no connections yet.')}</div><div class="social-core__detail-section"><div class="social-core__section-head"><div><span>ACTIVITY</span><h2>Community activity</h2></div></div>${data.activity.map(x=>detail(x.activity_type||'Community activity',x.metadata?.message||'A community action was recorded.',date(x.created_at))).join('')||empty('No recent community activity.')}</div></section>`;if(view==='discover')return `<section class="social-core__detail-grid"><div class="social-core__detail-section"><div class="social-core__section-head"><div><span>PLACES</span><h2>Discoveries</h2></div><strong>${data.locations.length}</strong></div>${data.locations.map(x=>detail(x.name||'Location',x.address||x.city||'Nearby place',x.place_type||'Place')).join('')||empty('No discoverable places are available yet.')}</div><div class="social-core__detail-section"><div class="social-core__section-head"><div><span>OPPORTUNITIES</span><h2>Events & offers</h2></div></div>${data.events.map(x=>detail(x.title||'Event',x.description||'Community event',x.event_date||'')).join('')}${data.promotions.map(x=>detail(x.title||'Promotion',x.description||x.discount||'Business offer',x.ends_at?`Ends ${date(x.ends_at)}`:'')).join('')||empty('No active events or promotions yet.')}</div></section>`;if(view==='compete')return `<section class="social-core__detail-grid"><div class="social-core__detail-section"><div class="social-core__section-head"><div><span>COMPETE</span><h2>Games & challenges</h2></div></div>${data.games.map(x=>detail(x.name,x.description,`${x.difficulty||'easy'} · ${x.reward_points||0} XP`)).join('')}${data.challenges.map(x=>detail(x.name,x.description,`${x.period||'weekly'} · ${x.reward_points||0} XP`)).join('')||empty('No active games or challenges yet.')}</div><div class="social-core__detail-section"><div class="social-core__section-head"><div><span>CONTESTS</span><h2>Community contests</h2></div><strong>${data.contests.length}</strong></div>${data.contests.map(x=>detail(x.name,x.description||'Community contest',`${x.status} · ends ${date(x.ends_at)}`)).join('')||empty('No active contests yet.')}</div></section>`;if(view==='messages')return `<section class="social-core__detail-section"><div class="social-core__section-head"><div><span>INBOX</span><h2>Conversations</h2></div><strong>${data.messages.length}</strong></div>${data.messages.map(x=>detail(x.from_id===state.user?.id?'You':'Community member',x.content||'Message',`${x.status||'sent'} · ${date(x.created_at)}`)).join('')||empty('No messages yet.')}</section>`;return `<section class="social-core__detail-section"><div class="social-core__section-head"><div><span>UPDATES</span><h2>Notifications</h2></div><strong>${data.notifications.length}</strong></div>${data.notifications.map(x=>detail(x.title||x.type||'Community update',x.body||'Community activity update.',date(x.created_at))).join('')||empty('You are all caught up.')}</section>`;}
  function renderFeed(host){if(!host)return;const posts=state.posts||[];host.innerHTML=posts.length?posts.map(p=>`<article class="social-core__post"><div class="social-core__post-meta"><strong>${esc(p.kind||'community')}</strong><span>${date(p.created_at)}</span></div><p>${esc(p.content||'')}</p>${p.media_url?`<img src="${esc(p.media_url)}" alt="Community post media">`:''}</article>`).join(''):`<section class="social-core__empty"><div class="social-core__empty-icon">◎</div><h2>Your community starts here.</h2><p>Share a restroom discovery, helpful tip, verification, route win or local find.</p><button type="button" data-action="compose">Share something</button></section>`;host.querySelector('[data-action="compose"]')?.addEventListener('click',()=>openComposer());}
  function openComposer(){if(!state.user){alert('Sign in to post to the community.');return;}const d=document.createElement('dialog');d.className='social-core__dialog';d.innerHTML='<form method="dialog" class="social-core__composer"><div class="social-core__composer-head"><h2>Share with Kleenest</h2><button class="social-core__close" value="cancel">×</button></div><select name="kind"><option value="discovery">Discovery</option><option value="tip">Helpful tip</option><option value="verification">Verification</option><option value="win">Community win</option></select><textarea name="content" required placeholder="What should the community know?"></textarea><div class="social-core__composer-actions"><button value="cancel">Cancel</button><button value="default" type="submit">Post</button></div></form>';document.body.append(d);d.showModal();d.querySelector('form').onsubmit=async e=>{e.preventDefault();const f=new FormData(d.querySelector('form'));try{await createPost({content:f.get('content'),kind:f.get('kind')});d.close();d.remove();await render();}catch(err){alert(err?.message||'Unable to post.')}};d.addEventListener('close',()=>d.remove());}
  async function render(){if(state.destroyed)return;await Promise.all([loadSummary(),getPosts()]);const content=await panel(state.view);root.replaceChildren();const shell=document.createElement('section');shell.className='social-core';shell.innerHTML=`<header class="social-core__header"><div class="social-core__eyebrow">KLEENEST COMMUNITY</div><div class="social-core__header-row"><div><h1>Social</h1><p>${esc(VIEWS[state.view][1])}</p></div><button type="button" class="social-core__share" data-action="compose"><span>+</span> Share</button></div></header>${progressCard()}<nav class="social-core__nav" aria-label="Social sections">${Object.entries(VIEWS).map(([key,meta])=>`<button type="button" class="${state.view===key?'active':''}" data-view="${key}">${meta[0]}</button>`).join('')}</nav><div class="social-core__body">${content}</div>`;root.append(shell);shell.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',async()=>{state.view=b.dataset.view;await render();}));shell.querySelector('[data-action="compose"]')?.addEventListener('click',openComposer);if(state.view==='feed')renderFeed(shell.querySelector('[data-region="feed"]'));}
  function destroy(){state.destroyed=true;root.replaceChildren();}
  return Object.freeze({render,destroy,createPost});
}
