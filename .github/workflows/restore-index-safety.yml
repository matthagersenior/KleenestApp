name: Disabled legacy index safety restore

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy monolith safety restoration is intentionally disabled."
