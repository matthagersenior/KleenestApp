/* Maps Navigation UI: real navigation state presentation; no simulated instructions. */
export function createMapsNavigationUI({root,navigation,onSpeak=()=>{}}={}){if(!root||!navigation)throw new Error('Navigation UI requires a root and navigation core.');let lastStepId=null;function render(state={}){const m=state.maneuver;const step=m?.step;const distance=m?.distanceMeters;const stop=state.nextStop?.locations||state.nextStop||{};root.innerHTML=`<section class="maps-nav-card" aria-live="polite"><div class="maps-nav-card__head"><span>LIVE NAVIGATION</span><strong>${state.routeCompleted?'Destination reached':state.arrived?'Stop reached':state.active?'Following route':'Route ready'}</strong></div><div class="maps-nav-card__maneuver"><div class="maps-nav-card__icon">${escapeHtml(icon(step?.maneuver?.type,step?.maneuver?.modifier))}</div><div><small>Next maneuver</small><strong>${escapeHtml(maneuverText(step))}</strong><span>${distance==null?'':formatDistance(distance)}${step?.name?' · '+escapeHtml(step.name):''}</span></div></div><div class="maps-nav-card__progress"><span style="width:${Math.max(0,Math.min(100,(state.progress||0)*100))}%"></span></div><div class="maps-nav-card__meta"><span>${Math.round((state.progress||0)*100)}% route complete</span>${state.distanceToNextStopMeters!=null?`<span>${formatDistance(state.distanceToNextStopMeters)} to ${escapeHtml(stop.name||'next stop')}</span>`:''}</div></section>`;if(step&&m?.stepIndex!==lastStepId){lastStepId=m.stepIndex;onSpeak(maneuverText(step)+(step?.name?` on ${step.name}`:''))}}return Object.freeze({render})}function maneuverText(s){if(!s)return'Continue on route';const t=s.maneuver?.type||'continue',m=s.maneuver?.modifier;const map={turn:`Turn ${m||''}`,new_name:'Continue',depart:'Start route',arrive:'Arrive',roundabout:`Take the roundabout${m?` ${m}`:''}`,merge:`Merge${m?` ${m}`:''}`,fork:`Keep ${m||'ahead'}`,end_of_road:`Turn ${m||'ahead'}`,on_ramp:'Take the ramp',off_ramp:'Take the exit'};return (map[t]||'Continue') .replace(/\s+/g,' ').trim()}function icon(t,m){if(t==='arrive')return'⌖';if(t==='roundabout')return'↻';if(m==='left'||m==='sharp left')return'↰';if(m==='right'||m==='sharp right')return'↱';if(m==='uturn')return'↶';return'↑'}function formatDistance(m){if(m<1000)return`${Math.round(m)} m`;return`${(m/1000).toFixed(1)} km`}function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}