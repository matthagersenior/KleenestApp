/* Maps Verification Module: canonical bathroom verification RPC. */
export function createMapsVerification({supabase,progression=null,user=null}={}){async function verify({locationId,evidence={},method='user'}={}){if(!user?.id)throw new Error('Authentication is required for verification.');const lat=evidence.lat??null,lng=evidence.lng??null;const hasBathroom=Boolean(evidence.hasPublicBathroom??evidence.has_bathroom);const {data,error}=await supabase.rpc('record_bathroom_verification',{p_location_id:locationId,p_has_public_bathroom:hasBathroom,p_lat:lat,p_lng:lng,p_distance_meters:evidence.distanceMeters??null});if(error)throw error;await progression?.record?.({metric:'location_verified',source_type:'verification',source_id:locationId,payload:{method,hasBathroom}});return data}return Object.freeze({verify})}