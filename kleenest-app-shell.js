/* Authoritative modular shell. Feature modules are loaded only for the surface being opened. */
(function(){'use strict';
 if(window.__KLEENEST_SHELL_BOOTED)return;window.__KLEENEST_SHELL_BOOTED=true;
 const root=()=>document.getElementById('modular-root');let view='Home',loading=false;
 const reg=()=>window.KleenestFeatureRegistry;
 const load=async n=>{if(!reg())throw Error('Feature registry unavailable');return reg().load(n)};
 const loadMany=async a=>{for(const n of a)await load(n)};
 const loadScript=src=>new Promise((ok,bad)=>{if(document.querySelector('script[data-kleenest-shell-dep="'+src+'"]'))return ok();const s=document.createElement('script');s.src=src;s.dataset.kleenestShellDep=src;s.onload=ok;s.onerror=()=>bad(Error('Unable to load '+src));document.head.appendChild(s)});
 async function core(){await load('core');await load('events');await load('runtime');}
 async function authApi(){await core();if(!window.supabase?.createClient)await loadScript('https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2');if(!window.KleenestSupabase)await loadScript('kleenest-supabase.js?auth=20260815');}
 function auth(){const old=document.getElementById('kleenest-direct-auth');if(old)old.remove();const el=document.createElement('div');el.id='kleenest-direct-auth';el.style.cssText='position:fixed;inset:0;z-index:2147483647;background:rgba(0,0,0,.55);display:flex;align-items:center;justify-content:center;padding:20px';el.innerHTML='<div style="width:min(440px,100%);background:#fff;border-radius:18px;padding:24px;color:#16211d"><button type="button" data-close style="float:right;border:0;background:none;font-size:26px">×</button><h2 id="at">Sign in to Kleenest</h2><p id="as" class="muted">Sign in to access your account.</p><form id="af"><label>Email<input required type="email" name="email" style="display:block;width:100%;padding:10px;margin-top:5px"></label><label>Password<input required minlength="6" type="password" name="password" style="display:block;width:100%;padding:10px;margin-top:5px"></label><div id="ae" style="display:none;color:#a33;margin-top:10px"></div><button id="ab" class="primary" style="width:100%;margin-top:14px">Sign In</button></form><button id="asw" style="width:100%;margin-top:9px;border:0;background:none;color:#0e7c6b">Need an account? Sign up</button></div>';document.body.appendChild(el);let signup=false;const form=el.querySelector('#af'),btn=el.querySelector('#ab');el.querySelector('[data-close]').onclick=()=>el.remove();el.querySelector('#asw').onclick=()=>{signup=!signup;el.querySelector('#at').textContent=signup?'Create your Kleenest account':'Sign in to Kleenest';btn.textContent=signup?'Create Account':'Sign In'};form.onsubmit=async e=>{e.preventDefault();const err=el.querySelector('#ae');try{btn.disabled=true;await authApi();const f=new FormData(form);const r=signup?await KleenestSupabase.signUp(f.get('email'),f.get('password'),{}):await KleenestSupabase.signIn(f.get('email'),f.get('password'));if(signup&&!r?.session){err.textContent='Account created. Check your email, then sign in.';err.style.display='block';return}const s=await KleenestSupabase.session();window.KleenestRuntime=window.KleenestRuntime||{};window.KleenestRuntime.user=s?.user||null;el.remove();await surface()}catch(x){err.textContent=x.message||'Authentication failed';err.style.display='block'}finally{btn.disabled=false;btn.textContent=signup?'Create Account':'Sign In'}};}
 const user=()=>window.KleenestRuntime?.user||null;const isBiz=()=>!!(user()?.isAdmin||user()?.businessMembership||user()?.business_id||['growth','enterprise'].includes(String(user()?.businessTier||'').toLowerCase()));
 function header(){const tabs=['Home','Maps','Social','Profile'];if(isBiz())tabs.push('Business');if(user()?.isAdmin)tabs.push('Admin');return `<header class="top-nav" style="display:flex"><div class="brand">Kleenest</div><nav class="nav-links">${!user()?'<button class="nav-btn" data-auth>Sign In / Sign Up</button>':''}${tabs.map(t=>`<button class="nav-btn ${view===t?'active':''}" data-view="${t}">${t}</button>`).join('')}</nav></header>`}
 function base(){if(view==='Home')return '<section class="stack"><div class="card accent"><h1>Find the Kleenest places nearby.</h1><p class="muted">Discover locations, check in, review, play games, earn rewards and connect with the community.</p><button class="primary" data-view="Maps">Open Maps</button> <button class="secondary" data-view="Social">Explore Social</button></div><div class="card"><h3>Account</h3><p class="muted">Sign in to access your account and premium features.</p><button class="primary" data-auth>Sign In / Sign Up</button></div></section>';if(view==='Maps')return '<section class="stack"><div class="card"><h1>Nearby locations</h1><p class="muted">Loading locations and community data…</p><div id="map-results"></div></div></section>';if(view==='Social')return '<section class="stack"><div class="card"><h1>Social & Games</h1><p class="muted">Community activity, reviews, rewards and game progress.</p><div id="social-results"></div></div></section>';if(view==='Profile')return `<section class="card"><h1>Profile</h1><p class="muted">${user()?.email||'Sign in to access your account.'}</p></section>`;return `<section class="stack"><div id="surface-feature"></div></section>`}
 async function surface(){if(loading)return;loading=true;const r=root();try{await core();r.innerHTML=header()+'<main id="modular-workspace">'+base()+'</main>';bind();const w=document.getElementById('modular-workspace');if(view==='Maps'){await loadMany(['location','mapFilters','maps','mapCompat']);const rows=window.KleenestData?.restrooms||window.KleenestState?.restrooms||[];document.getElementById('map-results').innerHTML=rows.length?rows.slice(0,30).map(x=>`<div class="card"><b>${esc(x.name||'Location')}</b><div class="muted">${esc(x.address||x.city||'Nearby location')}</div></div>`).join(''):'<p class="muted">Location discovery is ready; no nearby records are currently loaded.</p>';window.KleenestMapDiscovery?.load?.('maps-open');}else if(view==='Social'){await loadMany(['reviews','rewards']);const rr=window.KleenestRewards?.state||{};document.getElementById('social-results').innerHTML='<div class="card"><h3>Community</h3><p>Reviews, rewards, check-ins and community participation are connected to this surface.</p><p class="muted">Rewards history and review tools are available through the modular services.</p></div>';}else if(view==='Business'){await loadMany(['business','businessAnalytics','qr','businessActions','businessManagement']);const B=window.KleenestBusinessWorkspace;if(B?.mount)await B.mount(w);else if(B?.render)B.render(w);}else if(view==='Admin'){await loadMany(['businessAdmin','businessAnalytics','qr']);const A=window.KleenestBusinessAdminComplete;if(A?.admin)await A.admin(w);else w.innerHTML='<div class="card"><h1>Admin</h1><p>Admin module loaded.</p></div>';}}catch(e){console.error('[Kleenest] surface',view,e);const w=document.getElementById('modular-workspace');if(w)w.innerHTML=`<section class="card"><h1>${view}</h1><p class="muted">${e.message||'Feature failed to load.'}</p><button class="primary" data-view="Home">Back Home</button></section>`;bind()}finally{loading=false}}
 const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
 function bind(){const r=root();r?.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{view=b.dataset.view;surface()});r?.querySelectorAll('[data-auth]').forEach(b=>b.onclick=e=>{e.preventDefault();auth()});}
 window.KleenestAppShell={navigate:v=>{view=v;surface()},render:surface,openAuth:auth};
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',surface,{once:true});else surface();
})();