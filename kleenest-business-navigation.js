/* Business/Admin navigation bridge for the modular shell. */
(function(){'use strict';
 function isAllowed(){const u=window.KleenestRuntime?.user;return !!(u?.isAdmin||window.KleenestBusinessWorkspace?.allowed?.());}
 function isAdmin(){const u=window.KleenestRuntime?.user;const m=window.KleenestBusinessWorkspace?.getContext?.()?.membership;return !!(u?.isAdmin||['owner','admin','manager'].includes(String(m?.role||'').toLowerCase())||String(u?.subscriptionTier||'').toLowerCase()==='enterprise');}
 function inject(){const nav=document.querySelector('header nav');if(!nav||!isAllowed())return;['Business',...(isAdmin()?['Admin']:[])].forEach(label=>{if(nav.querySelector(`[data-business-view="${label}"]`))return;const b=document.createElement('button');b.textContent=label;b.dataset.businessView=label;b.className='business-nav-button';nav.appendChild(b);});}
 async function open(label){const root=document.getElementById('modular-root');if(!root)return;document.querySelectorAll('header nav button').forEach(b=>b.classList.remove('active'));const b=document.querySelector(`[data-business-view="${label}"]`);if(b)b.classList.add('active');await window.KleenestBusinessWorkspace?.mount?.(root);if(label==='Admin'){const ctx=window.KleenestBusinessWorkspace?.getContext?.();root.querySelector('h1')?.insertAdjacentHTML('afterbegin','');const title=root.querySelector('h1');if(title)title.innerHTML='🛡️ Admin · '+title.textContent.replace('🏢 ','');}}
 document.addEventListener('click',e=>{const b=e.target.closest?.('[data-business-view]');if(b){e.preventDefault();e.stopImmediatePropagation();open(b.dataset.businessView);}},true);
 const obs=new MutationObserver(()=>inject());function start(){inject();const h=document.querySelector('header');if(h)obs.observe(h,{childList:true,subtree:true});window.addEventListener('kleenest:auth-ui-refresh',()=>setTimeout(inject,20));window.addEventListener('kleenest:auth-state-changed',()=>setTimeout(inject,20));}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();window.KleenestBusinessNavigation={inject,open};})();