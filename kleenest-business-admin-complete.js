/* Kleenest Business/Admin workspace. Content only; application shell owns navigation. */
(function(){'use strict';
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const rpc=(n,a={})=>window.KleenestSupabase.rpc(n,a);let businessId=null,ds='overview';
const A=()=>window.kleenestBusinessAnalytics;
const label=k=>A()?.labels?.[k]||String(k).replaceAll('_',' ');
const fmt=(k,v)=>{if(v===null||v===undefined)return'N/A';if(k==='average_rating')return Number(v).toFixed(2)+'/5';if(k.endsWith('_pct')||k==='conversion_rate_pct'||k==='percentile')return Number(v).toFixed(2)+'%';if(k==='revenue'||k==='cost_cents')return'$'+(Number(v)/100).toFixed(2);if(k==='total_bytes'||k==='avg_bytes')return(Number(v)/1048576).toFixed(2)+' MB';if(k==='peak_hour')return String(v).padStart(2,'0')+':00';return Number.isFinite(Number(v))?Number(v).toLocaleString():String(v)};
function metric(labelText,value){return `<div class="stat"><b>${esc(value??0)}</b>${esc(labelText)}</div>`}
function metrics(data,key){const x=data?.dataset||data||{};const fields=A()?.fields?.[key]||Object.keys(x);return `<div class="stats">${fields.map(k=>`<div class="stat"><b>${esc(fmt(k,x[k]))}</b>${esc(label(k))}</div>`).join('')}</div>${x.roi_status?`<p class="muted" style="margin-top:10px">${esc(x.roi_status)}</p>`:''}`}
async function context(){const ms=await window.KleenestSupabase.businessMemberships();const m=(ms||[]).find(x=>['owner','admin','manager','analyst'].includes(String(x.role||'').toLowerCase()))||ms?.[0];businessId=m?.business_id||m?.businesses?.id||m?.business?.id;return{membership:m,business:m?.businesses||m?.business||null}}
async function loadDataset(root){const out=root.querySelector('#biz-metrics');out.innerHTML='<p class="muted">Loading authoritative analytics…</p>';try{const rows=await A()?.current?.(ds);const row=rows?.[0];if(row?.error)throw Error(row.error);out.innerHTML=metrics(row?.dataset||{},ds)}catch(e){out.innerHTML=`<p class="message-banner error">${esc(e.message||'Analytics unavailable')}</p>`}}
async function business(root){const c=await context();if(!c.business&&!businessId)throw Error('No business membership is available for this account.');const b=c.business||{id:businessId,name:'Business'};businessId=businessId||b.id;root.innerHTML=`<section class="stack"><div class="title-row"><div><h1>🏢 ${esc(b.name||'Business')}</h1><p class="muted">${esc(c.membership?.role||'Owner')} · ${esc(b.business_tier||'Enterprise')}</p></div><button class="ghost" id="biz-refresh">↻ Refresh</button></div><div class="card"><label style="font-weight:800">Analytics dataset</label><select id="biz-dataset" class="input">${A().datasets.map(x=>`<option value="${x}">${x==='qr'?'QR & Check-ins':x[0].toUpperCase()+x.slice(1)}</option>`).join('')}</select><div id="biz-metrics" style="margin-top:12px"></div></div><div class="card"><h3>Business operations</h3><div class="actions" style="margin-top:10px"><button class="primary" data-biz="location">Add Location</button><button class="secondary" data-biz="promotion">Create Promotion</button><button class="secondary" data-biz="program">Create Partner Program</button><button class="secondary" data-biz="partner">Find Partner</button><button class="secondary" data-biz="campaign">Create Campaign</button></div><div id="biz-panel" style="margin-top:12px"></div></div><div class="card"><h3>QR & Media</h3><p class="muted">Manage location QR check-ins, official photos and eligible 360°/VR media.</p><div class="actions" style="margin-top:10px"><button class="primary" data-biz="qr">Manage QR</button><button class="secondary" data-biz="media">Manage Photos / VR</button></div></div></section>`;const sel=root.querySelector('#biz-dataset');sel.value=ds;sel.onchange=async e=>{ds=e.target.value;await loadDataset(root)};root.querySelector('#biz-refresh').onclick=()=>business(root);root.querySelectorAll('[data-biz]').forEach(btn=>btn.onclick=()=>operation(btn.dataset.biz,root));await loadDataset(root)}
async function operation(type,root){const p=root.querySelector('#biz-panel');if(type==='promotion'){p.innerHTML='<form><input class="input" name="name" placeholder="Promotion name" required><textarea class="input" name="description" placeholder="Description"></textarea><input class="input" name="start" type="datetime-local"><input class="input" name="end" type="datetime-local"><button class="primary">Create Promotion</button></form>';p.querySelector('form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await rpc('business_create_promotion',{p_business_id:businessId,p_name:f.get('name'),p_description:f.get('description'),p_start_at:f.get('start')?new Date(f.get('start')).toISOString():new Date().toISOString(),p_end_at:f.get('end')?new Date(f.get('end')).toISOString():null);p.innerHTML='<p>Promotion created.</p>'}catch(err){p.innerHTML=`<p class="message-banner error">${esc(err.message)}</p>`}}}
else if(type==='program'){p.innerHTML='<form><input class="input" name="name" placeholder="Program name" required><button class="primary">Create Partner Program</button></form>';p.querySelector('form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await rpc('business_create_partner_program',{p_name:f.get('name')});p.innerHTML='<p>Partner program created.</p>'}catch(err){p.innerHTML=`<p class="message-banner error">${esc(err.message)}</p>`}}}
else if(type==='location'){p.innerHTML='<form><input class="input" name="name" placeholder="Location name" required><input class="input" name="address" placeholder="Address" required><input class="input" name="city" placeholder="City"><input class="input" name="state" placeholder="State"><button class="primary">Add Location</button></form>';p.querySelector('form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await rpc('business_create_location',{p_business_id:businessId,p_name:f.get('name'),p_address:f.get('address'),p_city:f.get('city'),p_state:f.get('state')});p.innerHTML='<p>Location created.</p>'}catch(err){p.innerHTML=`<p class="message-banner error">${esc(err.message)}</p>`}}}
else if(type==='partner'){p.innerHTML='<h3>Find Partner</h3><div id="partners">Loading…</div>';try{const rows=await rpc('business_list_partner_programs');p.querySelector('#partners').innerHTML=(rows||[]).map(x=>`<div class="place"><div><strong>${esc(x.name||'Partner Program')}</strong></div><button class="primary" data-request="${esc(x.id)}">Request</button></div>`).join('')||'<p class="muted">No eligible partner programs found.</p>';p.querySelectorAll('[data-request]').forEach(btn=>btn.onclick=async()=>{try{await rpc('business_request_partner_agreement',{p_partner_program_id:btn.dataset.request,p_partner_business_id:businessId});btn.textContent='Requested';btn.disabled=true}catch(err){p.insertAdjacentHTML('beforeend',`<p class="message-banner error">${esc(err.message)}</p>`)}})}catch(err){p.innerHTML=`<p class="message-banner error">${esc(err.message)}</p>`}}
else if(type==='campaign'){p.innerHTML='<form><input class="input" name="network" placeholder="Partner network ID" required><input class="input" name="name" placeholder="Campaign name" required><select class="input" name="type"><option value="engagement">Engagement</option><option value="promotion">Promotion</option><option value="check_in">Check-in</option><option value="review">Review</option></select><textarea class="input" name="goal" placeholder="Goal"></textarea><button class="primary">Create Campaign</button></form>';p.querySelector('form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await rpc('create_enterprise_partner_campaign',{p_network_id:f.get('network'),p_name:f.get('name'),p_campaign_type:f.get('type'),p_goal:f.get('goal')});p.innerHTML='<p>Campaign created.</p>'}catch(err){p.innerHTML=`<p class="message-banner error">${esc(err.message)}</p>`}}}
else if(type==='qr'){p.innerHTML='<h3>QR Management</h3><button class="primary" id="qr-refresh">Load Locations</button>';p.querySelector('#qr-refresh').onclick=async()=>{try{const rows=await rpc('business_list_locations',{p_business_id:businessId});p.innerHTML=(rows||[]).map(x=>`<div class="place"><div><strong>${esc(x.name)}</strong><span>${esc(x.address||'')}</span></div><button class="secondary" data-location="${esc(x.id)}">QR</button></div>`).join('')||'<p>No locations.</p>';p.querySelectorAll('[data-location]').forEach(btn=>btn.onclick=async()=>{const card=await window.KleenestQR?.businessCard?.(rows.find(x=>String(x.id)===String(btn.dataset.location)));p.insertAdjacentHTML('beforeend',card||'')})}catch(err){p.innerHTML=`<p class="message-banner error">${esc(err.message)}</p>`}}}
else if(type==='media'){p.innerHTML='<h3>Business Media</h3><p class="muted">Official photos and 360°/VR media are tracked with size metadata.</p><p class="muted">Use the Photos / VR upload workflow to attach media to a location.</p>';}}
async function admin(root){let d={};try{d=await rpc('admin_get_overview')}catch(e){throw e}const nums=['businesses','users','active_users','reports','events','checkins','reviews','campaigns'];root.innerHTML=`<section class="stack"><div class="title-row"><h1>🛡️ Admin</h1></div><div class="card"><h3>Platform overview</h3><div class="stats" style="margin-top:12px">${nums.map(k=>metric(k.replaceAll('_',' '),d?.[k]??d?.summary?.[k]??0)).join('')}</div></div><div class="card"><h3>Promote / authorize an account</h3><form id="admin-cap"><input class="input" name="user" placeholder="User UUID" required><select class="input" name="tier"><option value="free">Free</option><option value="premium">Premium</option><option value="business">Business</option><option value="enterprise">Enterprise</option></select><select class="input" name="role"><option value="customer">Customer</option><option value="member">Business Member</option><option value="manager">Manager</option><option value="owner">Business Owner</option><option value="admin">Admin</option></select><label><input type="checkbox" name="business"> Business account</label> <label><input type="checkbox" name="isadmin"> Admin</label><button class="primary">Apply Capabilities</button></form><div id="admin-result"></div></div></section>`;root.querySelector('#admin-cap').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await rpc('admin_set_account_capabilities',{p_target_user_id:f.get('user'),p_role:f.get('role'),p_subscription_tier:f.get('tier'),p_is_business_user:f.has('business'),p_is_admin:f.has('isadmin'),p_is_demo_test:true,p_reason:'Kleenest feature testing'});root.querySelector('#admin-result').innerHTML='<p>Capabilities updated successfully.</p>'}catch(err){root.querySelector('#admin-result').innerHTML=`<p class="message-banner error">${esc(err.message)}</p>`}}}
window.KleenestBusinessAdminComplete={business,admin};})();